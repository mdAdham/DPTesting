class Account:
    balance = 3000
    
    def MaincardWithdraw(self, amount):
        if self.balance >= amount:
            balence -= amount
            
    def SecondcardWithdraw(self, amount):
        if self.balance >= amount:
            balence -= amount
            
acc = Account()
acc.MaincardWithdraw(2500)
acc.SecondcardWithdraw(500)